<div>
  <%= form_with model: [book, book_comment], local: false do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに", class: "form-control comment_textarea" %>
  </div>
    <%= f.submit "送信" %>
  <% end %>
</div>